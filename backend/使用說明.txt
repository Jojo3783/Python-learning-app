1.先創建虛擬環境 (anaconda之類的)
2.pip 指令 : pip install -r "..../requirements.txt"
  複製requirements確切的位置，如果要新增庫，直接在requirements.txt裡加上名字，重跑上面那行，版本盡量加。
3.環境建好後，確保進入backend資料夾，在terminal執行 : uvicorn main:app --reload
4.可以進http://127.0.0.1:8000/api看看有沒有東東  (http://127.0.0.1:8000/docs 這樣好像才有)

